<Window x:Class="LocalCallPro.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Local Call"
        SizeToContent="WidthAndHeight"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">

    <Border Background="#1E1E2E" BorderBrush="#BB86FC" BorderThickness="1"
            CornerRadius="10" Padding="18,14" MinWidth="320" MaxWidth="400">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                <Ellipse Width="8" Height="8" Fill="#BB86FC" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock x:Name="TxtTitle" FontSize="13" FontWeight="Bold"
                           Foreground="#BB86FC" VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock x:Name="TxtBody" Grid.Row="1"
                       FontSize="13" Foreground="#E0E0E0"
                       TextWrapping="Wrap" Margin="0,0,0,14"/>

            <StackPanel x:Name="ButtonPanel" Grid.Row="2"
                        Orientation="Horizontal" HorizontalAlignment="Right"/>
        </Grid>
    </Border>
</Window>
